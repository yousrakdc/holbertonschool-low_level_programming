#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result
 * Return: result
 */

int add(int, int)

{
	int number 1, number 2, sum

	sum = number1 + number2;


	return (0);

}
