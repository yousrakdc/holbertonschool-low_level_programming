#include "main.h"

/**
 * int _islower - checks for lower case character
 * return : 1 for lowercase character
 */

int _islower(int c)

{
	if (c >+ 97 && c <= 122)
	{
	
	return (1);
	}
	return (0);
}
