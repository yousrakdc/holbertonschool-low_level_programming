#include "main.h"
#include <stdio.h>

/**
 * times_table - prints the 9 times table, starting with 0
 * @n:  number 
 * Return: always success
 */

void times_table(void)

}
	int n;

	for (int i = 1; i <= 9; ++i)
	{
	printf("%d * %d = %d \n", n, i, n * i);
	}
}
